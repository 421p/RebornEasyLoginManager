name: Build and Release

on:
  push:
    branches: [ "main", "master" ]
    tags: [ "v*" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install wxWidgets
        run: brew install wxwidgets
      - name: Configure CMake
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
      - name: Build
        run: cmake --build build --config Release
      - name: Create Bundle
        run: |
          mkdir -p EasyLoginManager.app/Contents/MacOS
          mkdir -p EasyLoginManager.app/Contents/Resources
          cp build/EasyLoginManager EasyLoginManager.app/Contents/MacOS/
          
          # Generate icon
          brew install imagemagick
          magick -size 1024x1024 xc:skyblue -gravity center -pointsize 400 -font Helvetica -fill white -draw "text 0,0 'ELM'" icon.png
          mkdir icon.iconset
          sips -z 16 16     icon.png --out icon.iconset/icon_16x16.png
          sips -z 32 32     icon.png --out icon.iconset/icon_16x16@2x.png
          sips -z 32 32     icon.png --out icon.iconset/icon_32x32.png
          sips -z 64 64     icon.png --out icon.iconset/icon_32x32@2x.png
          sips -z 128 128   icon.png --out icon.iconset/icon_128x128.png
          sips -z 256 256   icon.png --out icon.iconset/icon_128x128@2x.png
          sips -z 256 256   icon.png --out icon.iconset/icon_256x256.png
          sips -z 512 512   icon.png --out icon.iconset/icon_256x256@2x.png
          sips -z 512 512   icon.png --out icon.iconset/icon_512x512.png
          sips -z 1024 1024 icon.png --out icon.iconset/icon_512x512@2x.png
          iconutil -c icns icon.iconset
          cp icon.icns EasyLoginManager.app/Contents/Resources/
          
          # Create a minimal Info.plist
          cat <<EOF > EasyLoginManager.app/Contents/Info.plist
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key>
              <string>EasyLoginManager</string>
              <key>CFBundleIdentifier</key>
              <string>com.l2reborn.easyloginmanager</string>
              <key>CFBundleName</key>
              <string>EasyLoginManager</string>
              <key>CFBundlePackageType</key>
              <string>APPL</string>
              <key>CFBundleIconFile</key>
              <string>icon.icns</string>
              <key>LSMinimumSystemVersion</key>
              <string>10.13</string>
          </dict>
          </plist>
          EOF

          # Bundle dependencies
          brew install dylibbundler
          dylibbundler -od -b -x EasyLoginManager.app/Contents/MacOS/EasyLoginManager -d EasyLoginManager.app/Contents/libs/
          
          # Create DMG with Applications shortcut
          mkdir -p dmg_root
          cp -R EasyLoginManager.app dmg_root/
          ln -s /Applications dmg_root/Applications
          hdiutil create -volname "EasyLoginManager" -srcfolder dmg_root -ov -format UDZO EasyLoginManager.dmg
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: EasyLoginManager-macOS
          path: EasyLoginManager.dmg

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1
      - name: Generate Icon
        run: |
          magick -size 256x256 xc:skyblue -gravity center -pointsize 100 -font Helvetica -fill white -draw "text 0,0 'ELM'" icon.png
          magick icon.png -define icon:auto-resize=256,128,64,48,32,16 icon.ico
      - name: Cache vcpkg
        uses: actions/cache@v4
        with:
          path: |
            C:/vcpkg/installed
            C:/vcpkg/downloads
          key: ${{ runner.os }}-vcpkg-${{ hashFiles('vcpkg.json') || 'default' }}
          restore-keys: |
            ${{ runner.os }}-vcpkg-
      - name: Install wxWidgets (vcpkg)
        run: |
          vcpkg install wxwidgets:x64-windows-static --triplet x64-windows-static
      - name: Configure CMake
        run: |
          cmake -S . -B build `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_TOOLCHAIN_FILE="$env:VCPKG_INSTALLATION_ROOT/scripts/buildsystems/vcpkg.cmake" `
            -DVCPKG_TARGET_TRIPLET=x64-windows-static
      - name: Build
        run: cmake --build build --config Release
      - name: Package
        run: |
          mkdir dist
          copy build\Release\EasyLoginManager.exe dist\
          cd dist
          7z a ..\EasyLoginManager-Windows.zip EasyLoginManager.exe
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: EasyLoginManager-Windows
          path: EasyLoginManager-Windows.zip

  release:
    needs: [build-macos, build-windows]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download Windows Artifact
        uses: actions/download-artifact@v4
        with:
          name: EasyLoginManager-Windows
      - name: Download macOS Artifact
        uses: actions/download-artifact@v4
        with:
          name: EasyLoginManager-macOS
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            EasyLoginManager-Windows.zip
            EasyLoginManager.dmg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
